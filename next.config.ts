import type { NextConfig } from "next";

/**
 * Конфигурация Next.js для проекта.
 *
 * `cacheComponents: true` включает Cache Components, необходимые для директивы `"use cache"`,
 * `cacheLife()` и `cacheTag()` в `app/products`.
 *
 * @example
 * // Без cacheComponents: true директива "use cache" не будет работать как Cache Components API.
 */
const nextConfig: NextConfig = {
  /* config options here */
  reactCompiler: true,
  output: "standalone",
  allowedDevOrigins: ["127.0.0.1", "127.0.2.2", "172.16.5.89"],

  /**
   * Отключает HTTP-заголовок "X-Powered-By: Next.js".
   *
   * Это небольшая мера безопасности:
   * злоумышленникам становится сложнее определить,
   * на какой технологии работает приложение.
   */
  poweredByHeader: false,

  /**
   * Включает React Strict Mode.
   *
   * Используется только в режиме разработки.
   *
   * Strict Mode помогает находить:
   * - небезопасные побочные эффекты;
   * - устаревшие API;
   * - проблемы с жизненным циклом компонентов;
   * - ошибки при повторном рендеринге.
   *
   * В production на работу приложения не влияет.
   */
  reactStrictMode: true,

  /**
   * Запрещает генерацию Source Maps
   * для production-сборки.
   *
   * Благодаря этому:
   * - уменьшается размер сборки;
   * - скрывается исходный код приложения;
   * - усложняется анализ приложения после деплоя.
   */
  productionBrowserSourceMaps: false,

  /**
   * Отключает встроенное gzip-сжатие Next.js.
   *
   * Обычно используется, если приложение работает
   * за обратным прокси (Nginx, Cloudflare, Vercel),
   * который самостоятельно выполняет сжатие.
   */
  compress: false,

  /**
   * Отключает генерацию HTTP-заголовков ETag.
   *
   * ETag используется браузером для проверки,
   * изменился ли ресурс.
   *
   * Иногда отключается при использовании CDN,
   * который самостоятельно занимается кэшированием.
   */
  generateEtags: false,

  /**
   * Перед новой сборкой автоматически очищает
   * папку .next.
   *
   * Позволяет избежать использования
   * устаревших артефактов предыдущей сборки.
   */
  cleanDistDir: true,

  /**
   * Настройки компилятора TypeScript.
   */
  typescript: {
    /**
     * Игнорирует ошибки TypeScript во время build.
     *
     * Даже если имеются ошибки типов,
     * приложение будет успешно собрано.
     *
     * Использовать рекомендуется только
     * в исключительных случаях.
     */
    ignoreBuildErrors: true,
  },

  /**
   * Настройки логирования Next.js.
   */
  logging: {
    /**
     * Настройки логирования запросов fetch().
     */
    fetches: {
      /**
       * Показывать полный URL каждого fetch-запроса
       * в консоли разработчика.
       *
       * Полезно при отладке Server Components,
       * Route Handlers и Server Actions.
       */
      fullUrl: true,
    },
  },

  /**
   * Экспериментальные возможности Next.js.
   *
   * Эти функции могут измениться
   * в будущих версиях фреймворка.
   */
  experimental: {
    /**
     * Автоматически оптимизирует импорт
     * компонентов из указанных библиотек.
     *
     * Вместо подключения всей библиотеки
     * Next.js импортирует только используемые
     * модули.
     *
     * Это позволяет:
     * - уменьшить размер JavaScript-бандла;
     * - ускорить tree shaking;
     * - уменьшить время сборки;
     * - ускорить запуск приложения.
     *
     * Особенно полезно для библиотек,
     * содержащих большое количество экспортов.
     */
    optimizePackageImports: [
      /**
       * Библиотека SVG-иконок.
       */
      "lucide-react",

      /**
       * Библиотека для работы с датами.
       */
      "date-fns",
    ],
  },
  images: {
    /**
     * Форматы изображений, которые Next.js будет использовать
     * при оптимизации.
     */
    formats: ["image/avif", "image/webp"],

    /**
     * Минимальное время хранения оптимизированных изображений
     * в кеше CDN (в секундах).
     *
     * 31536000 = 1 год.
     */
    minimumCacheTTL: 31_536_000,

    /**
     * Способ отображения изображения браузером.
     *
     * inline — открывать изображение прямо в браузере.
     */
    contentDispositionType: "inline",

    /**
     * Набор поддерживаемых размеров экранов.
     *
     * Используется компонентом next/image
     * для генерации адаптивных изображений.
     */
    deviceSizes: [320, 420, 640, 750, 828, 1080, 1200, 1440, 1920, 2560, 3840],

    /**
     * Дополнительные размеры изображений
     * для иконок, аватаров и небольших элементов интерфейса.
     */
    imageSizes: [12, 16, 24, 32, 48, 64, 96, 128, 256, 512],

    /**
     * Разрешенные локальные изображения.
     *
     * Все изображения внутри /assets/avatars
     * смогут использоваться через next/image.
     */
    // localPatterns: [
    //   {
    //     pathname: "/assets/avatars/**",
    //   },
    // ],

    /**
     * Список внешних ресурсов,
     * изображения с которых разрешено оптимизировать.
     */
    remotePatterns: [
      {
        protocol: "https",
        hostname: "images.unsplash.com",
        port: "",
      },
      {
        protocol: "https",
        hostname: "**.kinopoisk.ru",
        port: "",
      },
    ],
  },
};

export default nextConfig;

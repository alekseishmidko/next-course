/**
 * Modern-страница магазина.
 *
 * Используется как маршрут `/store/modern` для демонстрации новой версии store-flow рядом с
 * legacy-вариантом `/store/legacy`.
 */
export default function ModernPage() {
  return <div>ModernPage</div>;
}

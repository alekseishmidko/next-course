/**
 * Страница входа в аккаунт.
 *
 * Рендерится внутри layout из группы `(auth)`, поэтому получает общую auth-обертку без добавления
 * `(auth)` в URL.
 */
export default function LoginPage() {
  return <div>Login Page</div>;
}
